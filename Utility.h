#include <iostream>
int mirrorRetarget(int gridLimit, int coord);
int torusRetarget(int gridLimit, int coord);
bool stringIsInt(std::string userString);
bool getUserBool(std::string prompt, std::string boolTrue, std::string boolFalse);
float getUserFloat(std::string prompt);
int getUserInt(std::string prompt);
void clearConsole();
